import type { NavMenuItem } from "lib/functions/nav-menu-item.model";
/**
 * Handles NavMenuItem.action if it exists
 * @param el
 * @param item
 * @returns
 */
export declare function handleNavItemAction(el: HTMLElement, item: NavMenuItem): void;
